# Variables de entorno para Railway
# Usa estos valores para configurar en el dashboard o con CLI

# JWT
JWT_SECRET=change-me-in-production-very-secure-secret-key-123456789
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Server
PORT=3000
NODE_ENV=production

# CORS (ajusta según tu dominio)
CORS_ORIGIN=*

# Logging
LOG_LEVEL=info

# DATABASE_URL se configura automáticamente cuando agregas PostgreSQL
# Debe ser: DATABASE_URL=${{Postgres.DATABASE_URL}}

